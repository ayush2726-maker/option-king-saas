[phases.setup]
nixPkgs = ["python311", "openssl"]

[phases.install]
cmds = ["python -m venv --copies /opt/venv", ". /opt/venv/bin/activate && pip install -r requirements.txt"]

[start]
cmd = "uvicorn alexa_app:app --host 0.0.0.0 --port $PORT"
